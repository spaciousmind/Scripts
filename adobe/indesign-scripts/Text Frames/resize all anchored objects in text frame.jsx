function main() {  var frame = app.selection[0],      matrix = app.transformationMatrices.add({horizontalScaleFactor: 0.75, verticalScaleFactor: 0.75});//include      matrix = app.transformationMatrices.add({verticalTranslation: -.5}); //for moving up and down   if (frame instanceof TextFrame) {    frame.pageItems.everyItem().transform(CoordinateSpaces.INNER_COORDINATES, AnchorPoint.CENTER_ANCHOR, matrix);  }} if (app.selection.length) {  app.doScript(main, ScriptLanguage.JAVASCRIPT, undefined, UndoModes.ENTIRE_SCRIPT, "Scale Inline Page Items to 75%");}